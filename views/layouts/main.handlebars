<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>collabHub | Where Developers Connect</title>

    <!-- Fontawesome -->
    <script src="https://kit.fontawesome.com/0075d73424.js" crossorigin="anonymous"></script>
    <!-- Materialize -->
    <!-- Compiled and minified CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">

    <!-- Compiled and minified JavaScript -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
    <style>
        .nav-wrapper {
            background-color: gray;
            font-family: 'Segoe UI', 'Arial', 'sans-serif';
            font-size: 20px;
            background-position: center;
            min-height: 180px;
        }
        .container{
            margin-top:108px;
        }
        #l{
            margin-top: -77px;
        }
        #s{
            text-align: center;
            margin-top: -77px;
        }
        
        .brand-logo {
            margin-right: 40px;
            margin-left: 40px;
            padding-right: 10px;
        }

        .slogan {
            margin: auto;
            text-align: center;
        }

        .dark-grey-text {
            font-family: 'Segoe UI', Arial, sans-serif;
        }

        .id {
            font-family: 'Segoe UI', Arial, sans-serif;
        }
    </style>

    <script
    src="https://code.jquery.com/jquery-3.4.1.js"
    integrity="sha256-WpOohJOqMqqyKL9FccASB9O0KwACQJpFTUBLTYOVvVU="
    crossorigin="anonymous"></script>

</head>

<body>

    <nav>
        <div class="nav-wrapper">
            <div class="row">
                <a href="/login" class="brand-logo"> <i class="fab fa-connectdevelop"></i> collabHub</a>
                <a href="#" data-target="mobile-demo" class="sidenav-trigger"><i class="material-icons">menu</i></a>
                <ul class="right hide-on-med-and-down">
                    <li><a href="/login">Home</a></li>
                    <li><a href="/login">Login</a></li>
                    <li><a href="/signup">Sign up</a></li>
                    <li id="logout"><a>Logout</a></li>
                    <li><a href="/search">Search</a></li>

                </ul>
                <br style="line-height:0px;" />
                <ul class="slogan" class="center hide-on-med-and-down" style="height:20px;" >
                    <h3>Where Developers Connect</h3>
                </ul>
                </div>
        </div>
    </nav>
    
    {{{ body }}}
    
        
    <script>

        const logout = document.getElementById("logout")
        logout.addEventListener("click", () => {
            //logout user & end session
            console.log("try to hit some three pointers")
            $(location).attr("href", "/logout")
        })

$("#login").on("click", function (event) {
  event.preventDefault();
//create user object via email and password input
  var user = {
    email: $("#email").val().trim(),
    account_key: $("#password").val().trim()
  }
  
  $.post("/login", user, function(results){
    if(results){
       $(location).attr('href', '/profile')
     }else {
      $("#account-info").modal("close");
      alert("oops something went wrong, please try again!");
     }
  })
});
    </script>
</body>

</html>